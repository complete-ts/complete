{
  "$schema": "https://raw.githubusercontent.com/complete-ts/complete/refs/heads/main/packages/complete-cli/schemas/package-metadata-schema.json",
  "dependencies": {
    "typescript": {
      "lock-version": true,
      "lock-reason": "TSESLint does not work yet with TypeScript 7."
    }
  },
  "devDependencies": {
    "@babel/core": {
      "lock-version": true,
      "lock-reason": "Keep Babel on version 7 because Jest/Bun currently resolves Babel 7 transformer internals that are incompatible with Babel 8 presets."
    },
    "@babel/preset-env": {
      "lock-version": true,
      "lock-reason": "Keep Babel on version 7 because Jest/Bun currently resolves Babel 7 transformer internals that are incompatible with Babel 8 presets."
    },
    "@babel/preset-typescript": {
      "lock-version": true,
      "lock-reason": "Keep Babel on version 7 because Jest/Bun currently resolves Babel 7 transformer internals that are incompatible with Babel 8 presets."
    },
    "markdown-it": {
      "lock-version": true,
      "lock-reason": "markdownlint 0.41.1 uses markdown-it 14 types that are incompatible with markdown-it 15."
    }
  }
}
